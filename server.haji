let method = CTX["method"]
let path = CTX["path"]
let response = {"status": 404, "body": "Not Found"}

if (path == "/") {
    let html = json_read("index.html")
    let response = {"status": 200, "body": html}
}

if (path == "/api/cv") {
    let data = json_read("data/file.json")
    let response = {"status": 200, "body": data}
}